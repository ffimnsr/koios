# Koios configuration file.
# Generated by: koios init

[server]
listen_addr = %s
request_timeout = %s
allowed_origins = %s

%s%s

[session]
max_messages = %d
retention = %s
max_entries = %d
idle_reset_after = %s
idle_prune_after = %s
idle_prune_keep = %d
daily_reset_time = %s
prune_keep_tool_messages = %d

[compaction]
threshold = %d
reserve = %d

[memory]
embed_model = %s
inject = %t
top_k = %d

[memory.milvus]
address = %s
collection = %s
enabled = %t

[cron]
max_concurrent = %d

[heartbeat]
enabled = %t
every = %s

[agent]
max_children = %d
retry_attempts = %d
retry_initial_backoff = %s
retry_max_backoff = %s
retry_status_codes = [%s]

[tools]
profile = %s
allow = [%s]
deny = [%s]

[tools.exec]
enabled = %t
enable_deny_patterns = %t
custom_deny_patterns = [%s]
custom_allow_patterns = [%s]
default_timeout = %s
max_timeout = %s
approval_mode = %s
approval_ttl = %s

[tools.exec.isolation]
enabled = %t
expose_paths = [%s]

[workspace]
root = %s
per_agent = %t
max_file_bytes = %d

[log]
level = %s

[monitor]
stale_threshold = %s
max_restarts = %d
